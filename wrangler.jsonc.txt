{
  "$schema": "./node_modules/wrangler/config-schema.json",

  // The name of your Worker on Cloudflare. This becomes part of your
  // default URL: https://tagpulse-ai.<your-subdomain>.workers.dev
  "name": "tagpulse-ai",

  // The backend file that handles /api/generate, /api/verify-license,
  // and routes everything else to the static site.
  "main": "worker.js",

  // Keep this as today's date when you first deploy. Wrangler uses it
  // to lock in Workers runtime behavior so future platform changes
  // don't unexpectedly affect this project.
  "compatibility_date": "2026-08-07",

  // Static assets (index.html and anything else in public/) are
  // served automatically. run_worker_first ensures requests to
  // /api/* always run worker.js instead of being treated as a static
  // file lookup — everything else is served directly from public/
  // without even touching the Worker, which is the most cost-efficient
  // setup.
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "run_worker_first": ["/api/*"]
  }

  // GEMINI_API_KEY is intentionally NOT set here. Secrets should never
  // live in a config file that might get committed to git. Instead,
  // set it with `wrangler secret put GEMINI_API_KEY` — see README.md.
}
